{
  "name": "work-schedule-server",
  "version": "1.0.0",
  "description": "Parses a Coffee Bean & Tea Leaf schedule photo (ADP screenshot or printed sheet) into structured shifts using Claude's vision model, for an iOS Shortcut to add to Calendar.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "express": "^4.21.1",
    "multer": "^2.2.0"
  }
}
